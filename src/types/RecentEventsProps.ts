import { Dispatch, SetStateAction } from 'react';

export type RecentEventsProps = {
  hiddenSetLeft: string;
  activeSetEvents: string;
  firstText: string;
  secondText: string;
  thirdText: string;
};
